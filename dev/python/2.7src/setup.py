#!/usr/bin/python

from distutils.core import setup

setup(
      name = "karaoke",
      version = "0.1",
      description = "Karaoke player",
      author = 'Quan Ngo',
      packages = ['distutils', 'distutils.command']
      )